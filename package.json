{
  "name": "@khoralabs/memories",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "biome": "biome check .",
    "check": "biome check .",
    "check:write": "biome check --write .",
    "format": "biome check --write .",
    "format:check": "biome check .",
    "typecheck": "tsc --noEmit -p packages/persistence/core/tsconfig.json && tsc --noEmit -p packages/ontologies/tsconfig.json && tsc --noEmit -p packages/node/tsconfig.json && tsc --noEmit -p packages/service/tsconfig.json && tsc --noEmit -p packages/memories-agents/tsconfig.json && tsc --noEmit -p packages/react/graph/tsconfig.json",
    "test": "bun test",
    "prepare": "husky",
    "context": "bun run scripts/context.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/bun": "latest",
    "@types/parse-gitignore": "^1.0.2",
    "husky": "^9.1.7",
    "minimatch": "^10.2.5",
    "parse-gitignore": "^2.0.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*",
    "packages/**/*"
  ]
}
