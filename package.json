{
  "name": "@khoralabs/memories",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "biome": "biome check .",
    "check": "biome check .",
    "check:write": "biome check --write .",
    "format": "biome check --write .",
    "format:check": "biome check .",
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/autolink/tsconfig.json && tsc --noEmit -p packages/persistence/sqlite/tsconfig.json && tsc --noEmit -p packages/persistence/sqlite-graph-projections/tsconfig.json && tsc --noEmit -p packages/react/graph/tsconfig.json && tsc --noEmit -p packages/agents/adapter/tsconfig.json && tsc --noEmit -p packages/agents/integrator/tsconfig.json && tsc --noEmit -p packages/agents/investigator/tsconfig.json && tsc --noEmit -p packages/agents/tools/tsconfig.json",
    "test": "bun test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*",
    "packages/**/*"
  ]
}
